<UserControl x:Class="WPFDashboard.Views.OrderViews.OrderDetailsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:detailviewmodel="clr-namespace:WPFDashboard.ViewModel.DetailViewModels"
             xmlns:detailview="clr-namespace:WPFDashboard.Views.DetailViews"
             xmlns:local="clr-namespace:WPFDashboard.Views.OrderViews"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="800"
              DataContext="{Binding Source={StaticResource Locator}, Path=OrderDetails}">
    <Grid Background="LavenderBlush">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <!--======================ORDER DETAILS====================-->
            <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="2*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!--======First Line description=============-->
            <StackPanel Orientation="Horizontal" Margin="10">
                <Label Content="Order ID:" FontWeight="Bold"/>
                <Label Content="here should be ID"/>
            </StackPanel>

            <Label Grid.Column="1" Content="Content" Margin="10" FontWeight="Bold" HorizontalAlignment="Left"/>

            <StackPanel Grid.Column="1" Margin="10" Orientation="Horizontal" HorizontalAlignment="Right">
                <Label Content="State" FontWeight="Bold" HorizontalAlignment="Right"/>
                <ComboBox/>
            </StackPanel>

            <!--=========Content View Data Grid=========-->
            <!--<Viewbox >-->
            <DataGrid  Grid.Column="1" Grid.Row="1" AutoGenerateColumns="False" ItemsSource="{Binding OrderContents}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Product name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Amount" Binding="{Binding Amount}"/>
                    <DataGridTemplateColumn Header="Take action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Viewbox>
                                    <Button CommandParameter="{Binding}" Command="{Binding DataContext.BtnDelete,RelativeSource={RelativeSource AncestorType=DataGrid}}">
                                        <materialDesign:PackIcon Kind="Delete" VerticalAlignment="Center"/>
                                    </Button>
                                </Viewbox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Take action">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Viewbox DataContext="{Binding Source={StaticResource Locator}}">
                                    <Button Command="{Binding DataContext.BtnDetails,RelativeSource={RelativeSource AncestorType=DataGrid}}" CommandParameter="{Binding}">
                                        <materialDesign:PackIcon Kind="Details" VerticalAlignment="Center"/>
                                    </Button>
                                </Viewbox>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <!--</Viewbox>-->

            <!--==========Customer Data Details==========-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="3*"/>
                </Grid.RowDefinitions>
                <Label Content="Customer Name:" VerticalAlignment="Center"  HorizontalAlignment="Right"/>
                <Label Grid.Row="1" Content="Order Date:" VerticalAlignment="Center"  HorizontalAlignment="Right"/>
                <Label Grid.Row="2" Content="Due Date:" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <!--Content of Customer details-->
                <Label Content="Max Musterman" Grid.Column="1" VerticalAlignment="Center"  HorizontalAlignment="Left" />
                <Label Grid.Row="1" Content="dd.mm.yyyy" Grid.Column="1" VerticalAlignment="Center" HorizontalAlignment="Left" />
                <Viewbox Grid.Row="2" Grid.Column="1">
                    <Calendar/>
                </Viewbox>
            </Grid>

            <!--=========Comment========-->
            <Viewbox Grid.Row="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Viewbox>
                        <Label Content="Comment" FontWeight="Bold"/>
                    </Viewbox>
                    <TextBox Grid.Row="1" Margin="5"/>
                </Grid>
            </Viewbox>
            <!--Comment to Pause + Save Button-->
            <Viewbox Grid.Column="1" Grid.Row="2">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="2*"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Label Content="Description to paused"/>
                    <Button Grid.Row="1" Width="150">
                        <Grid Width="110">
                            <materialDesign:PackIcon Kind="ContentSaveAll" VerticalAlignment="Center"/>
                            <TextBlock Text="Save" HorizontalAlignment="Center"/>
                        </Grid>
                    </Button>
                </Grid>
            </Viewbox>
        </Grid>

        <!--=================PACKAGE OR CHOCOLATE DETAILS==============-->
        <!--Package Detailspage-->
        <ContentControl Grid.Column="1" Content="{Binding PackageDetail}">

            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type detailviewmodel:PackageDetailVm}">
                    <detailview:PackageDetailView></detailview:PackageDetailView>
                </DataTemplate>
            </ContentControl.Resources>
           

        </ContentControl>
    </Grid>
</UserControl>
