<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:WPFDashboard.Views.DetailViews"
             xmlns:Converter="clr-namespace:WPFDashboard.Helpers.Converter" x:Class="WPFDashboard.Views.DetailViews.CreationDetailsView"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="800"
              >

    <UserControl.Resources>
        <Converter:BoolToStatusConverter x:Key="BoolToStatusConverter"/>
    </UserControl.Resources>
    <UserControl.DataContext>
        <Binding Path="CreationDetail" Source="{StaticResource Locator}"/>
    </UserControl.DataContext>
    
        <Grid Margin="10">
            <!--Title row-->
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Label Background="Beige" Content="Creation Details" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="15" FontWeight="Bold"/>

            <!--Details window-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="1.5*"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="2*"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="2*"/>
                </Grid.RowDefinitions>


                


                <!--=============CHOCO NAME, WRAPPING,SHAPE AND PRICE SECTIONS===============-->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition />
                        <RowDefinition />
                        <RowDefinition />
                       
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!--CHOCOLATE NAME-->
                    <Label Content="Name:" FontWeight="Bold" FontSize="12" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    <Label Grid.Column="1" Content="{Binding CurrentOrderChocolate.Name}" Margin="5" FontSize="12" HorizontalAlignment="Left" VerticalAlignment="Center"/>

                    <!--WRAPPING TEXT-->
                    <Label Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Right" Content="Wrapping:" FontWeight="Bold" FontSize="12" Margin="5"/>
                    <Label VerticalAlignment="Center" Content="{Binding CurrentOrderChocolate.Wrapping.Name}" Grid.Column="1" Grid.Row="1" Margin="1"/>

                    <!--SHAPE TEXT-->
                    <Label Grid.Row="2" VerticalAlignment="Center" HorizontalAlignment="Right" Content="Shape:" Margin="5" FontWeight="Bold" FontSize="12"/>
                    <Label VerticalAlignment="Center" Content="{Binding CurrentOrderChocolate.Shape.Name}" Grid.Row="2" Grid.Column="1"/>

                    

                </Grid>

                <!--DESCRIPTIONS-->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
                <!--STATE-->
                <Label VerticalAlignment="Center" Content="State" FontWeight="Bold" HorizontalAlignment="Right" FontSize="12"/>
                <ComboBox Grid.Column="1" Width="100" Height="30" Margin="5" SelectedIndex="1" ItemsSource="{Binding AvailabilityStates}" SelectedItem="{Binding CurrentState}"/>
                
                <!--DESCRIPTION-->
                <Label Grid.Row="1"  Content="Description:" Margin="5" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <Label Grid.Row="1" Grid.Column="1" Content="{Binding CurrentOrderChocolate.Description}" Margin="5" VerticalAlignment="Center"/>
              
                <!--Price-->
                <Label Grid.Row="5" Content="Price:" Margin="5" FontWeight="Bold" FontSize="12" VerticalAlignment="Center" HorizontalAlignment="Right"/>
                <Label  Grid.Row="5" Grid.Column="1" VerticalAlignment="Center" FontWeight="Bold" Content="{Binding CurrentOrderChocolate.Price}"/>
            </Grid>
            <!--=================DATA GRID WITH INGREDIENT LIST===========-->

                <Label Content="Ingredients List:" Grid.Row="1" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center" FontWeight="Bold" FontSize="12"/>
                <DataGrid ItemsSource="{Binding Ingredients}" Margin="10" Grid.Row="2" Grid.ColumnSpan="2" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="State" Binding="{Binding Available}" IsReadOnly="True" />
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" IsReadOnly="True" />
                        <DataGridTextColumn Header="Type" Binding="{Binding Type}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Unit Type" Binding="{Binding UnitType}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Modified" Binding="{Binding Modified}" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" IsReadOnly="True" MaxWidth="450" />
                    </DataGrid.Columns>
                </DataGrid>


                <!--COMMENTS-->
                <Label Content="Comments:" FontSize="12" FontWeight="Bold" Grid.Row="3" Grid.ColumnSpan="2" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                <DataGrid ItemsSource="{Binding Comments}" Grid.Row="4" Grid.ColumnSpan="2" AutoGenerateColumns="False" CanUserAddRows="False">
                    <DataGrid.Columns>
                        <DataGridCheckBoxColumn Header="Published" IsReadOnly="True" Binding="{Binding Published}"/>
                        <DataGridTextColumn Header="Name:" Binding="{Binding Customer.FirstName}"/>

                        <DataGridTextColumn Header="Comment:" Binding="{Binding Comment}"/>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date}"/>
                        <DataGridTemplateColumn Header="Rating">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <materialDesign:RatingBar Value="{Binding Value}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

        </Grid>
    
</UserControl>
